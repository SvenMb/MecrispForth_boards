\ changes byteorder for 4x6 font provided with mecrisp
: recode ( font-addr )

    begin
        CR 
        dup h@ . ." h,  $" \ char #
        dup 2 + c@ h.2
        dup 3 + c@ h.2
        dup 4 + c@ h.2
        dup 5 + c@ h.2
        ."  ,"
    dup h@ while
        6 +
    repeat
    drop
;
